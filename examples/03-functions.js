// 03-functions — arrows and basic calls

let add = (a, b) => a + b;
let sum = add(10, 32); // 42

let inc = (x) => x + 1;
let incd = inc(41); // 42

let double = (x) => x * 2;
let doubled = double(21); // 42

sum + incd + doubled
